# CLAUDE.md

Read `docs/agent-quickstart.md` first — the minimal, high-signal guide for
agents working in this repo: core entry points, common task recipes, build
shortcuts, and **live UI testing over WebView2 remote debugging** (how to
actually drive and verify the running app, since browser-automation "computer
use" tools cannot see this native window — see `tools/agent-ui-debug/README.md`
for the full workflow and script).

See `.github/copilot-instructions.md` for the fuller playbook: coding
conventions, DSP/messaging essentials, and the change checklist.

## Git

Never auto-commit. Always wait for explicit user approval before committing.
